name: Test di connessione
on: [workflow_dispatch]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Stampa un saluto
        run: echo "🚀 Il sistema funziona! GitHub sta comunicando correttamente."
